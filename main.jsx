import React from "react";
import ReactDOM from "react-dom/client";
import JobTracker from "./job_tracker.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <JobTracker />
  </React.StrictMode>
);
